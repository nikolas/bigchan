<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%= render 'layouts/stylesheets' %>
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <%= render 'layouts/ga' %>

  <%= csrf_meta_tag %> -->
</head>
<body>

  <center>
    <div class="b"><%= link_to 'b', root_path %></div>
    <iframe id="header_bubble" width="600" height="150" src="http://ada.evergreen.edu/~tc_nik/bigchan/">
      <h1>Bigchan</h1>
    </iframe>
    <div class="b"><%= link_to 'b', root_path %></div>
  </center>

  <div class="container">
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, :class => "flash #{key}") %>
    <% end %>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
  </div>

</body>
</html>
